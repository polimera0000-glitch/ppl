# Server
NODE_ENV=development
PORT=3000
API_VERSION=v1

DATABASE_URL =

# Postgres DB (local or remote)
# If you're running Postgres locally:
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=
DB_PASSWORD=
DB_NAME=
DB_NAME_TEST=

# Or, if using a hosted Postgres, set DB_HOST to its host and DB_SSL=true
# Example hosted: DB_HOST=db.example-host.com

# Optional single connection string (uncomment to use instead of separate vars)
# DATABASE_URL=postgres://postgres:your-password@localhost:5432/postgres

# JWT (keep)
JWT_SECRET=Y01W8jHJp75NjwQSFnCernCzLpBHRTiB
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# other env vars (optional)
MAX_FILE_SIZE=50MB
UPLOAD_PATH=uploads/

FRONTEND_BASE_URL=https://app.eph-platform.com
FRONTEND_URL=http://localhost:3001
DEEP_LINK_SCHEME=eph

EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_SMTP_SECURE=false   # true if using port 465 with TLS, false for 587 (STARTTLS)
EMAIL_USER=
EMAIL_PASSWORD=
EMAIL_FROM="EPH Platform <noreply@ephplatform.com>"

# Optional TLS behavior for dev (if connecting to dev smtp with self-signed cert)
EMAIL_TLS_REJECT_UNAUTHORIZED=false

# Google OAuth Configuration
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/api/v1/auth/google/callback

# GitHub OAuth Configuration
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_REDIRECT_URI=http://localhost:3000/api/v1/auth/github/callback

# Admin Configuration
# Set to 'true' only for initial setup - remove after first admin is created
CREATE_INITIAL_ADMIN=true
INITIAL_ADMIN_EMAIL=admin@ephplatform.com
INITIAL_ADMIN_NAME=EPH Platform Administrator
# IMPORTANT: Change this strong password and remove from env after first login
INITIAL_ADMIN_PASSWORD=EphAdmin2024!@

# Auth Service Configuration
ADMIN_MAGIC_LINK_EXPIRY_SECONDS=600
PASSWORD_RESET_EXPIRY_SECONDS=3600